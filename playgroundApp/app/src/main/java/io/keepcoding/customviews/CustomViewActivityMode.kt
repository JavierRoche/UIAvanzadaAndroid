package io.keepcoding.customviews

// Enumerado con los tipos de actividad para las vistas customizadas
enum class CustomViewActivityMode {
    FLAT_CUSTOM,
    COMPOSITE,
    TOUCH_CIRCLES,
    TOUCH_PATH
}
